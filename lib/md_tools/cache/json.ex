defmodule MdTools.Cache.Json do
  def read(_cachefile) do
  end

  def append(_cachefile, _data) do
  end

  def update(_cachefile, _docfile, _data) do
  end

  def delete(_cachefile, _docfile) do
  end

end
